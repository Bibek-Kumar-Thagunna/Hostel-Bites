// Admin panel component with modern design
import React from 'react';
import AdminDashboard from './AdminDashboard';

const AdminPanel = ({ userData }) => {
    return <AdminDashboard userData={userData} />;
};

export default AdminPanel;